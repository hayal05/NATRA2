import { invoke } from "@tauri-apps/api/core";
import Chart from "chart.js/auto";
const IN=new Set(["SALE","INCOME","PAYMENT","TRANSFER_IN"]), OUT=new Set(["PURCHASE","EXPENSE","REFUND","TRANSFER_OUT"]);
const money=n=>`ETB ${Number(n||0).toLocaleString("en-US",{maximumFractionDigits:2})}`;
const settings=()=>{try{return JSON.parse(localStorage.getItem("natraSettings")||"{}")}catch(_){return{}}};
const totals=rows=>(rows||[]).reduce((a,r)=>{const n=Number(r.amount||0),t=String(r.tx_type);if(IN.has(t))a.cashIn+=n;if(OUT.has(t))a.cashOut+=n;return a},{cashIn:0,cashOut:0});
async function refreshDashboardCash(){try{const root=document.querySelector("#page-dashboard"),t=totals(await invoke("list_transactions"));const k=root?.querySelectorAll(".kpi-value");if(k?.[3])k[3].textContent=money(t.cashIn-t.cashOut)}catch(_){} }
function lockStock(){const sku=document.querySelector("#fSku"),stock=document.querySelector("#fStock");if(!sku||!stock||!sku.disabled||stock.dataset.integrityLocked)return;stock.disabled=true;stock.dataset.integrityLocked="1";stock.title="Existing stock is controlled by Sales, Purchases, Returns and Stock Adjustments.";const l=stock.closest(".form-field")?.querySelector("label");if(l)l.textContent="Current stock (automatic)"}
function addPurchaseCategory(){const s=document.querySelector("#purchaseSku");if(!s||document.querySelector("#purchaseProductCategory"))return;const f=document.createElement("div");f.className="form-field";f.innerHTML='<label>Product category</label><input id="purchaseProductCategory" readonly aria-readonly="true">';s.closest(".form-field")?.after(f)}
async function purchaseCategory(){const s=document.querySelector("#purchaseSku"),c=document.querySelector("#purchaseProductCategory");if(!s||!c)return;try{const p=(await invoke("list_products")||[]).find(x=>x.sku===s.value);c.value=p?.category||""}catch(_){} }
let cashChart;
async function refreshCashFlow(){const page=document.querySelector("#page-cashflow.active");if(!page)return;try{const rows=await invoke("list_transactions"),t=totals(rows),stats=page.querySelector("#cfStats");if(stats)stats.innerHTML=`<div class="stat-card"><span>Cash balance</span><strong>${money(t.cashIn-t.cashOut)}</strong></div><div class="stat-card"><span>Cash In</span><strong>${money(t.cashIn)}</strong></div><div class="stat-card"><span>Cash Out</span><strong>${money(t.cashOut)}</strong></div><div class="stat-card"><span>Net Cash Flow</span><strong>${money(t.cashIn-t.cashOut)}</strong></div>`;const byDay={};for(const r of rows||[]){const d=String(r.created_at||"").slice(0,10);if(!d)continue;byDay[d]??={in:0,out:0};const n=Number(r.amount||0),type=String(r.tx_type);if(IN.has(type))byDay[d].in+=n;if(OUT.has(type))byDay[d].out+=n}const keys=Object.keys(byDay).sort().slice(-14),labels=keys.length?keys.map(d=>new Date(`${d}T00:00:00`).toLocaleDateString(undefined,{month:"short",day:"numeric"})):['No activity'];let running=0;const net=keys.map(k=>running+=byDay[k].in-byDay[k].out);cashChart?.destroy();const c=page.querySelector("#cfAccountingChart");if(c)cashChart=new Chart(c,{type:"line",data:{labels,datasets:[{label:"Cash In",data:keys.map(k=>byDay[k].in),tension:.3,borderWidth:2},{label:"Cash Out",data:keys.map(k=>byDay[k].out),tension:.3,borderWidth:2},{label:"Net Movement",data:net,tension:.3,borderWidth:2}]},options:{responsive:true,maintainAspectRatio:false,interaction:{mode:"index",intersect:false},plugins:{legend:{position:"bottom"}},scales:{y:{beginAtZero:true}}}});const source=page.querySelector("#cfSources");if(source){const byType=x=>(rows||[]).filter(r=>String(r.tx_type)===x).reduce((s,r)=>s+Number(r.amount||0),0);source.innerHTML=`<div class="cf-source"><span>Sales receipts</span><b>${money(byType("SALE"))}</b></div><div class="cf-source"><span>Customer payments</span><b>${money(byType("PAYMENT"))}</b></div><div class="cf-source"><span>Other income</span><b>${money(byType("INCOME"))}</b></div><div class="cf-source"><span>Supplier purchases</span><b>${money(byType("PURCHASE"))}</b></div><div class="cf-source"><span>Operating expenses</span><b>${money(byType("EXPENSE"))}</b></div><div class="cf-source"><span>Refunds</span><b>${money(byType("REFUND"))}</b></div><div class="cf-source"><span>Internal transfers</span><small>Excluded from net business cash flow because they only move money between business accounts.</small></div>`}}catch(_){} }
async function refreshTax(){const side=document.querySelector(".sidebar");if(!side)return;let card=side.querySelector(".natra-estimated-tax");if(!card){card=document.createElement("div");card.className="natra-estimated-tax";card.innerHTML='<div class="natra-estimated-tax-head"><b>Estimated Tax</b><span>Auto</span></div><div class="natra-estimated-tax-value" id="systemEstimatedTaxValue">ETB 0</div><div class="natra-estimated-tax-meta"><span>Profit basis</span><b id="systemTaxProfit">ETB 0</b></div><div class="natra-estimated-tax-meta"><span>Rate</span><input id="systemTaxRate" type="number" min="0" max="100" step="0.01" aria-label="Estimated tax rate">%</div>';side.appendChild(card);const rate=card.querySelector("#systemTaxRate");rate.value=Number(settings().taxRate??localStorage.getItem("natra.estimatedTaxRate")??0);rate.addEventListener("change",()=>{const s=settings();s.taxRate=Math.min(100,Math.max(0,Number(rate.value)||0));localStorage.setItem("natraSettings",JSON.stringify(s));localStorage.setItem("natra.estimatedTaxRate",String(s.taxRate));refreshTax()})}try{const r=await invoke("report_summary"),profit=Math.max(0,Number(r?.profit||0)),rate=Math.min(100,Math.max(0,Number(card.querySelector("#systemTaxRate")?.value||0)));card.querySelector("#systemTaxProfit").textContent=money(profit);card.querySelector("#systemEstimatedTaxValue").textContent=money(profit*rate/100)}catch(_){} }
function style(){if(document.getElementById("natraIntegrityStyles"))return;const s=document.createElement("style");s.id="natraIntegrityStyles";s.textContent='.natra-estimated-tax{margin:10px 12px 4px;padding:10px 11px;border:1px solid rgba(148,163,184,.18);border-radius:11px;background:rgba(15,23,42,.28);color:#fff}.natra-estimated-tax-head{display:flex;justify-content:space-between;font-size:11px}.natra-estimated-tax-value{margin-top:5px;font-size:18px;font-weight:800}.natra-estimated-tax-meta{display:flex;justify-content:space-between;margin-top:4px;font-size:9px;color:#94a3b8}.natra-estimated-tax-meta b{color:#dbeafe}.natra-estimated-tax-meta input{width:42px;background:transparent;border:0;border-bottom:1px solid rgba(148,163,184,.35);color:#fff;text-align:right;font-size:9px;outline:none}.form-field input:disabled{opacity:.7;cursor:not-allowed}';document.head.appendChild(s)}
function boot(){style();setInterval(lockStock,1000);setInterval(()=>{refreshDashboardCash();refreshCashFlow();refreshTax()},5000);setInterval(()=>{addPurchaseCategory();purchaseCategory()},1000);refreshDashboardCash();refreshTax()}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot();

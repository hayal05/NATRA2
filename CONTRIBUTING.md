# Contributing to NATRA Management

Use feature branches and pull requests. Keep business calculations deterministic and covered by QA. Never commit credentials, production databases, or customer data.

Before a pull request:

```bash
npm install
npm run qa
npm run build
```
